# LLM / Agent surfaces
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Optional: if you run a local vector DB service
VECTOR_DB_URL=

# Repo target (if you run indexing tools)
TARGET_REPO_PATH=../

# Safety
SANDBOX_MODE=read-only   # read-only | write | network
MAX_AGENT_STEPS=40
