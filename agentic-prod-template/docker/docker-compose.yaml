version: "3.9"
services:
  runner:
    build:
      context: ..
      dockerfile: docker/Dockerfile.runner
    volumes:
      - ..:/workspace
    working_dir: /workspace
    command: ["bash", "-lc", "python -m tools.doctor"]
