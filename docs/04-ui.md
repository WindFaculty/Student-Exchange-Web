# UI Spec - Student Exchange

Updated: 2026-02-16

## Design Direction
- Clean admin/customer separation.
- Tailwind-based reusable UI (`Button`, `Input`, `Card`, `Badge`).
- Mobile-first layouts.

## Public Routes
- `/login`
- `/products`
- `/products/:id`
- `/cart`
- `/checkout`
- `/order-success/:orderCode`
- `/events`
- `/events/:id`
- `/iot`
- `/iot/projects/:slug`
- `/listings` (authenticated user, alias redirect to `/profile?tab=products`)
- `/profile` (authenticated user)
- `/support`
- `/support/faq`
- `/support/contact`
- `/support/track-order`
- `/support/policies`
- `/support/policies/purchase`
- `/support/policies/refund`

## Admin Routes
- `/admin/login`
- `/admin`
- `/admin/listings`
- `/admin/listings/new`
- `/admin/listings/:id/edit`
- `/admin/orders`
- `/admin/events`
- `/admin/tickets`
- `/admin/iot-content`
- `/admin/iot-sample-projects`
- `/admin/iot-sample-projects/new`
- `/admin/iot-sample-projects/:id/edit`

## Screen Requirements

### Listings / Cart / Checkout
- Listing grid with search + category + pagination.
- Listing detail with quantity and add-to-cart.
- Cart allows quantity update/remove.
- Checkout submits customer info and redirects to success page.

### Events
- Event list/detail from backend.
- Event registration form.
- Admin event page supports CRUD + registration list.

### Support
- FAQ page with search + category.
- Contact page creates ticket and shows ticket code.
- Track page supports both order and ticket tracking.

### Profile
- Header account name opens `/profile` after login.
- Profile supports tabs via query param: `account`, `orders`, `products` (`/profile?tab=products` opens My Products).
- Profile page shows account info (username, role, full name, email).
- Profile page supports structured Vietnam address input (`province`, `district`, `ward`, `addressLine`) with full-address preview.
- Profile page includes order tracking list with scope filter: `ACCOUNT`, `EMAIL`, `BOTH`.
- My Products tab supports create/update/delete and restore archived listings.

### Admin
- Dashboard with summary metrics.
- Dashboard includes on-demand Vietnam address sync card (`sync status`, `last source`, `sync counts`, sync button).
- Listings table + form.
- Orders table with status transition actions.
- Tickets table with status update + reply.

## Access Rules
- Unauthenticated users redirected to `/login` for protected user routes.
- Admin routes require `ADMIN` role; otherwise redirect.
