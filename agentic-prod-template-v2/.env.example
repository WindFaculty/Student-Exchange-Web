# LLM / Agent surfaces
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Optional: vector DB endpoint if your stack uses one
VECTOR_DB_URL=

# Repository target for indexing tools
TARGET_REPO_PATH=../

# Safety defaults
SANDBOX_MODE=read-only   # read-only | write | full
MAX_AGENT_STEPS=40
